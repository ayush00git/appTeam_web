<body>
<style>
  .error-box {
    position: absolute;
    top: 90px;
    color: #ffffff;
    z-index: 2;
    background-color: #201734;
    padding: 12px 16px;
    border-radius: 8px;
    border: 1px solid #382e57;
    box-shadow: 0 0 15px rgba(125, 94, 255, 0.6);
    font-weight: 500;
    margin-bottom: 15px;
    opacity: 1;
    transition: opacity 0.5s ease;
  }
</style>
<div class="error-box" id="errorBox"><%= error %></div>
<script>
  setTimeout(() => {
    const box = document.getElementById("errorBox");
    if (box) {
      box.style.opacity = "0";
      setTimeout(() => box.remove(), 500); // remove it after fade out
    }
  }, 1500);
</script>
</body>