<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>App Team</title>
    <link rel="shortcut icon" href="/favicon.png" type="image/x-icon" />
    <link
      href="https://fonts.googleapis.com/css2?family=Gasoek+One&display=swap"
      rel="stylesheet"
    />
    <link
      href="https://fonts.googleapis.com/css2?family=Ubuntu:wght@400;500;700&display=swap"
      rel="stylesheet"
    />
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: "ubuntu", sans-serif !important;
        background: linear-gradient(
          180deg,
          #140b29 0%,
          #140b29 60%,
          #a594f9 60%,
          #a594f9 100%
        );
        min-height: 100vh;
        overflow-x: hidden;
        user-select: none;
      }

      .container {
        width: 100%;
        min-height: 100vh;
      }

      /* Hero Section */
      .hero-section {
        height: 80vh;
        display: flex;
        align-items: center;
        justify-content: center;
        position: relative;
        padding: 0 2rem;
      }

      .hero-content {
        display: flex;
        align-items: center;
        justify-content: center;
        width: 100%;
        max-width: 1200px;
      }

      .hero-text {
        flex: 1; 
        text-align: center;
        animation: slideInLeft 1s ease-out;
      }

      .app-team-title {
        font-family: "Gasoek One", "sans-serif";
        font-size: clamp(4rem, 8vw, 8rem);
        font-weight: normal;
        color: #a594f9;
        line-height: 0.9;
        letter-spacing: -0.02em;
      }

      /* Domains Section */
      .domains-section {
        background: #a594f9;
        min-height: 40vh;
        padding: 4rem 2rem;
        clip-path: polygon(0 0, 100% 8%, 100% 100%, 0 100%);
        margin-top: -2vh;
      }

      .domains-header {
        text-align: center;
        margin-bottom: 3rem;
        animation: fadeInDown 1s ease-out 0.5s both;
      }

      .domains-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 2rem;
        max-width: 1200px;
        margin: 0 auto;
      }
      .domains-title {
        font-family: "Gasoek One", "sans-serif";
        font-size: clamp(2.5rem, 5vw, 4rem);
        color: #140b29;
        font-weight: normal;
      }

      .domain-card {
        position: relative;
        border-radius: 10px;
        overflow: hidden;
        box-shadow: 0 15px 35px rgba(20, 11, 41, 0.2);
        transition: transform 0.3s ease, box-shadow 0.3s ease;
        opacity: 0;
        transform: translateY(50px);
        height: 220px;
      }

      .domain-card:hover {
        transform: translateY(-10px) scale(1.02);
        box-shadow: 0 25px 50px rgba(20, 11, 41, 0.3);
      }

      .domain-image {
        width: 100%;
        height: 220px;
        object-fit: cover;
        transition: transform 0.3s ease;
        display: block;
        /* vertical-align: bottom; */
      }

      .domain-card:hover .domain-image {
        transform: scale(1.1);
      }

      .app {
        position: relative;
        width: 100%;
        min-height: 100vh;
        display: flex;
      }

      /* Left Panel - Fixed Purple Shape with Image */
      .left-panel {
        position: sticky;
        left: 0;
        top: 0;
        width: 50%;
        height: 100vh;
        z-index: 1;
        display: flex;
        align-items: center;
        justify-content: center;
        background-color: #140b29;
        flex-shrink: 0;
      }

      .purple-shape {
        position: absolute;
        width: 320px;
        height: 420px;
        background: #a594f9;
        z-index: 1;
        clip-path: polygon(0% 15%, 85% 0%, 100% 85%, 15% 100%);
        transform: rotate(-5deg);
      }

      .image-container {
        position: relative;
        z-index: 2;
        width: 260px;
        height: 360px;
        border-radius: 12px;
        overflow: hidden;
        box-shadow: 0 25px 50px rgba(0, 0, 0, 0.4);
        transition: all 0.8s ease-in-out;
        transform: rotate(-2deg);
      }

      .section-image {
        width: 100%;
        height: 100%;
        object-fit: cover;
        transition: all 0.8s ease-in-out;
        opacity: 1;
      }

      .section-image.changing {
        opacity: 0;
        transform: scale(1.05);
      }

      /* Right Panel - Scrollable Content */
      .right-panel {
        /* margin-left: 50%; */
        width: 50%;
        min-height: 100vh;
        background: #140b29;
        position: relative;
        z-index: 2;
      }

      .content-container {
        padding: 0;
        height: 100%;
      }

      /* Header */
      .header {
        height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 0 4rem;
        background: #140b29;
      }

      .main-title {
        font-size: 5rem;
        font-weight: 400;
        font-family: "Gasoek One", sans-serif;
        text-align: center;
        color: #a594f9;
        letter-spacing: 0.1em;
        text-transform: uppercase;
        line-height: 1.2;
      }

      /* Sections */
      .section {
        min-height: 80vh;
        padding: 4rem;
        display: flex;
        flex-direction: column;
        justify-content: center;
        transition: all 0.6s ease-in-out;
        position: relative;
      }

      .section.active {
        opacity: 1;
        transform: translateY(0);
      }

      .section.inactive {
        opacity: 0.3;
        transform: translateY(20px);
      }

      .section-title {
        font-size: 2.5rem;
        font-weight: 800;
        margin-bottom: 2rem;
        color: #a594f9;
        line-height: 1.3;
      }

      .section-description {
        font-size: 1.1rem;
        line-height: 1.8;
        color: #e2e8f0;
        margin-bottom: 3rem;
        max-width: 90%;
        font-weight: 400;
      }

      .section-divider {
        width: 100%;
        height: 2px;
        background: linear-gradient(90deg, #a594f9 0%, transparent 100%);
        margin-top: auto;
      }

      .scroll-spacer {
        height: 50vh;
      }

      .faq-container {
        min-height: 180vh;
        background-color: #a594f9;
      }

      /* Hero Section */
      .faq-hero-section {
        position: relative;
        min-height: 100vh;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        padding: 0 1rem;
        overflow: visible;
      }

      .faq-hero-content {
        position: relative;
        z-index: 2;
        text-align: center;
        transition: all 0.3s ease-out;
      }

      .faq-hero-title {
        font-family: "Gasoek One", cursive;
        font-size: clamp(3rem, 8vw, 6rem);
        font-weight: 400;
        margin-bottom: 2rem;
        color: #140b29;
        letter-spacing: -0.02em;
        line-height: 1.1;
        position: relative;
        z-index: 3;
      }

      .decorative-card {
        position: relative;
        margin: 0 auto;
        width: 320px;
        height: 192px;
        background: white;
        border-radius: 1rem;
        box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.25);
        overflow: hidden;
      }

      .card-background {
        position: absolute;
        inset: 0;
        background: linear-gradient(135deg, #f3e8ff, #dbeafe);
      }

      .decoration-1 {
        position: absolute;
        top: 1rem;
        left: 1rem;
        width: 3rem;
        height: 3rem;
        background: #fb923c;
        border-radius: 0.75rem;
        transform: rotate(12deg);
      }

      .decoration-2 {
        position: absolute;
        top: 2rem;
        right: 1.5rem;
        width: 2rem;
        height: 2rem;
        background: #f9a8d4;
        border-radius: 50%;
      }

      .decoration-3 {
        position: absolute;
        bottom: 1.5rem;
        left: 1.5rem;
        width: 4rem;
        height: 4rem;
        background: #60a5fa;
        border-radius: 1rem;
        transform: rotate(-6deg);
      }

      .main-icon-container {
        position: absolute;
        inset: 0;
        display: flex;
        align-items: center;
        justify-content: center;
      }

      .main-icon-bg {
        width: 5rem;
        height: 5rem;
        background: #e9d5ff;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        margin-bottom: 0.5rem;
      }

      .corner-icon {
        position: absolute;
        bottom: 1rem;
        right: 1rem;
        color: #9333ea;
      }

      .scroll-indicator {
        position: absolute;
        bottom: 2rem;
        animation: bounce 2s infinite;
      }

      @keyframes bounce {
        0%,
        20%,
        53%,
        80%,
        100% {
          transform: translateY(0);
        }
        40%,
        43% {
          transform: translateY(-10px);
        }
        70% {
          transform: translateY(-5px);
        }
      }

      /* FAQ Cards Section */
      .faq-section {
        position: relative;
        min-height: 100vh;
        padding: 5rem 1rem;
      }

      .faq-grid {
        max-width: 1200px;
        margin: 0 auto;
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        gap: 2rem;
      }

      .faq-card {
        aspect-ratio: 1;
        border-radius: 1rem;
        box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.1);
        transition: all 0.5s ease-out;
        cursor: pointer;
        overflow: hidden;
        display: flex;
        flex-direction: column;
      }

      .faq-card:hover {
        transform: scale(1.02) translateY(-8px);
      }

      .faq-card.security {
        background: #9333ea;
      }

      .faq-card.wallet {
        background: #10b981;
      }

      .faq-card.speed {
        background: #f97316;
      }

      .faq-card.support {
        background: #f87171;
      }

      .card-content {
        padding: 1.5rem;
        color: white;
        height: 100%;
        display: flex;
        flex-direction: column;
      }

      .card-header {
        display: flex;
        align-items: flex-start;
        gap: 1rem;
        margin-bottom: 1rem;
      }

      .icon-container {
        background: rgba(255, 255, 255, 0.2);
        padding: 0.75rem;
        border-radius: 0.5rem;
        flex-shrink: 0;
      }

      .card-question {
        font-size: 1.1rem;
        font-weight: 700;
        margin-bottom: 0.5rem;
        line-height: 1.2;
      }

      .card-answer {
        transition: all 0.5s ease-out;
        overflow: hidden;
        max-height: 0;
        opacity: 0;
        flex-grow: 1;
      }

      .faq-card:hover .card-answer {
        max-height: 200px;
        opacity: 1;
      }

      .answer-content {
        padding-top: 1rem;
        border-top: 1px solid rgba(255, 255, 255, 0.2);
      }

      .answer-text {
        color: rgba(255, 255, 255, 0.9);
        line-height: 1.6;
        font-size: 0.875rem;
      }

      .hover-indicator {
        margin-top: auto;
        display: flex;
        align-items: center;
        gap: 0.5rem;
        color: rgba(255, 255, 255, 0.7);
        font-size: 0.875rem;
        padding-top: 1rem;
      }

      .chevron {
        transition: transform 0.3s ease;
        width: 16px;
        height: 16px;
      }

      .faq-card:hover .chevron {
        transform: rotate(180deg);
      }

      .bottom-spacing {
        height: 1rem;
      }

      /* Icons */
      .icon {
        width: 24px;
        height: 24px;
        fill: currentColor;
      }

      .help-icon {
        width: 32px;
        height: 32px;
        color: #9333ea;
      }

      .help-icon-large {
        width: 40px;
        height: 40px;
        color: #9333ea;
      }

      /* Responsive Design */
      @media (max-width: 768px) {
        .faq-hero-title {
          font-size: 3rem;
          margin-bottom: 1.5rem;
        }

        .decorative-card {
          width: 280px;
          height: 168px;
        }

        .faq-grid {
          grid-template-columns: 1fr;
          max-width: 400px;
        }

        .card-content {
          padding: 1.5rem;
        }

        .card-question {
          font-size: 1.1rem;
        }
      }

      @media (min-width: 1200px) {
        .faq-hero-title {
          font-size: 6rem;
        }

        .faq-grid {
          grid-template-columns: repeat(4, 1fr);
          gap: 1.5rem;
        }
      }
      @media (min-width: 768px) and (max-width: 1199px) {
        .faq-grid {
          grid-template-columns: repeat(2, 1fr);
          gap: 1.5rem;
        }
      }
      /* Responsive Design */
      @media (max-width: 1024px) {
        .app {
          flex-direction: column;
        }

        .left-panel {
          position: relative;
          width: 100%;
          height: 50vh;
        }

        .purple-shape {
          width: 280px;
          height: 350px;
        }

        .image-container {
          width: 220px;
          height: 280px;
        }

        .right-panel {
          margin-left: 0;
          width: 100%;
        }

        .main-title {
          font-size: 3rem;
        }

        .section {
          padding: 2rem;
          min-height: 60vh;
        }

        .section-title {
          font-size: 2rem;
        }
      }

      @media (max-width: 768px) {
        .purple-shape {
          width: 240px;
          height: 300px;
        }

        .image-container {
          width: 180px;
          height: 240px;
        }

        .main-title {
          font-size: 4rem;
        }

        .section {
          padding: 1.5rem;
        }

        .section-title {
          font-size: 1.8rem;
        }

        .section-description {
          font-size: 1rem;
        }
      }

      /* Smooth scrolling with heavy feel */
      html {
        scroll-behavior: smooth;
      }

      body {
        scroll-behavior: smooth;
      }

      /* Custom scrollbar */
      ::-webkit-scrollbar {
        width: 8px;
      }

      ::-webkit-scrollbar-track {
        background: #140b29;
      }

      ::-webkit-scrollbar-thumb {
        background: #a594f9;
        border-radius: 4px;
      }

      ::-webkit-scrollbar-thumb:hover {
        background: #9084e8;
      }

      /* Animations */
      @keyframes slideInLeft {
        from {
          opacity: 0;
          transform: translateX(-100px);
        }
        to {
          opacity: 1;
          transform: translateX(0);
        }
      }

      @keyframes slideInRight {
        from {
          opacity: 0;
          transform: translateX(100px);
        }
        to {
          opacity: 1;
          transform: translateX(0);
        }
      }

      @keyframes fadeInDown {
        from {
          opacity: 0;
          transform: translateY(-50px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      @keyframes fadeInUp {
        from {
          opacity: 0;
          transform: translateY(50px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      .fade-in-up {
        animation: fadeInUp 0.8s ease-out forwards;
      }

      /* Responsive Design */
      @media (max-width: 768px) {
        .hero-content {
          flex-direction: column;
          text-align: center;
          gap: 2rem;
        }

        .domains-grid {
          grid-template-columns: 1fr;
          gap: 1.5rem;
        }

        .domains-section {
          padding: 3rem 1rem;
        }
      }

      @media (max-width: 480px) {
        .app-team-title {
          font-size: 5rem;
        }

        .domains-title {
          margin-top: 45px;
          font-size: 4rem;
        }
      }

      /* Scroll Effects */
      .scroll-reveal {
        opacity: 0;
        transform: translateY(50px);
        transition: all 0.6s ease-out;
      }

      .scroll-reveal.revealed {
        opacity: 1;
        transform: translateY(0);
      }
    </style>
  </head>
  <body>
    <%- include("./partials/nav") %>
    <div class="container">
      <section class="hero-section">
        <div class="hero-content">
          <div class="hero-text">
            <h1 class="app-team-title">APP TEAM</h1>
            <br />
          </div>
        </div>
      </section>

      <!-- Domains Section -->
      <section class="domains-section">
        <div class="domains-header">
          <h2 class="domains-title">Our Domains</h2>
        </div>
        <div class="domains-grid">
          <div class="domain-card" data-animate="fadeInUp">
            <img
              src="/homepage/web.webp"
              alt="Web Development"
              class="domain-image"
            />
          </div>
          <div class="domain-card" data-animate="fadeInUp" data-delay="100">
            <img
              src="/homepage/app.webp"
              alt="Mobile App Development"
              class="domain-image"
            />
          </div>
          <div class="domain-card" data-animate="fadeInUp" data-delay="200">
            <img
              src="/homepage/ml.webp"
              alt="AI & Machine Learning"
              class="domain-image"
            />
          </div>
          <div class="domain-card" data-animate="fadeInUp" data-delay="300">
            <img
              src="/homepage/bchain.webp"
              alt="Blockchain"
              class="domain-image"
            />
          </div>
          <div class="domain-card" data-animate="fadeInUp" data-delay="400">
            <img
              src="/homepage/ar.webp"
              alt="VR/AR Development"
              class="domain-image"
            />
            <div class="domain-title" style="color: #a594f9">AR/VR</div>
          </div>
          <div class="domain-card" data-animate="fadeInUp" data-delay="400">
            <img
              src="/homepage/iot.webp"
              alt="/ Development"
              class="domain-image"
            />
          </div>
        </div>
      </section>
    </div>
    <div class="app">
      <!-- Fixed Left Side - Purple Shape with Overlapping Image -->
      <div class="left-panel">
        <div class="purple-shape"></div>
        <div class="image-container">
          <img
            id="currentImage"
            src=""
            alt="Current section"
            class="section-image"
          />
        </div>
      </div>

      <!-- Right Side - Scrollable Content -->
      <div class="right-panel">
        <div class="content-container">
          <!-- Header -->
          <div class="header">
            <h1 class="main-title">WHAT WE DO</h1>
          </div>

          <!-- Sections -->
          <div class="section active" id="hack-on-hills">
            <h2 class="section-title">Conduct Hack on Hills</h2>
            <p class="section-description">
              North India's biggest student-run hackathon, powered by our robust
              event management and judging platform with real-time updates and
              seamless participant experience.
              <br />
              For more info about the upcoming hack-on-hills 7.0 visit the
              official website -
              <a
                href="https://www.hackonhills.com/"
                target="_blank"
                style="color: #a594f9"
                >HACKONHILLS-7.0</a
              >
            </p>
            <div class="section-divider"></div>
          </div>

          <div class="section inactive" id="nimbus-app">
            <h2 class="section-title">Nimbus App</h2>
            <p class="section-description">
              A comprehensive platform for NIT Hamirpur's annual tech fest,
              offering schedules, live updates, interactive maps, and
              streamlined event registration features.
              <br />
              Check out the official app of Nimbus 2k25 -
              <a
                href="https://play.google.com/store/apps/details?id=com.appteam.nimbus2k25&pcampaignid=web_share"
                target="_blank"
                style="color: #a594f9"
                >Nimbus-2k25</a
              >
            </p>
            <div class="section-divider"></div>
          </div>

          <div class="section inactive" id="hillfair-app">
            <h2 class="section-title">Hillfair App</h2>
            <p class="section-description">
              The official app for NIT Hamirpur's cultural fest, blending
              vibrant UI with seamless user experience to celebrate student
              creativity and cultural diversity through innovative features.
              <br />
              Check out the official app of Hillfair 2k24 -
              <a
                href="https://play.google.com/store/apps/details?id=com.appteam.hillfair2k24&pcampaignid=web_share"
                target="_blank"
                style="color: #a594f9"
                >Hillfair-2k24</a
              >
            </p>
            <div class="section-divider"></div>
          </div>

          <!-- Extra space for scrolling -->
          <div class="scroll-spacer"></div>
        </div>
      </div>
    </div>
    <div class="faq-container">
      <!-- Hero Section -->
      <div class="faq-hero-section">
        <div class="faq-hero-content" id="faqheroContent">
          <h1 class="faq-hero-title">Frequently asked questions</h1>

          <!-- Decorative Card -->
          <div class="decorative-card">
            <div class="card-background">
              <div class="decoration-1"></div>
              <div class="decoration-2"></div>
              <div class="decoration-3"></div>
              <div class="corner-icon">
                <svg
                  class="help-icon"
                  viewBox="0 0 24 24"
                  fill="none"
                  stroke="currentColor"
                  stroke-width="2"
                >
                  <circle cx="12" cy="12" r="10" />
                  <path d="m9,9a3,3 0 1,1 6,0c0,2 -3,3 -3,3" />
                  <path d="m12,17 l.01,0" />
                </svg>
              </div>
              <div class="main-icon-container">
                <div class="main-icon-bg">
                  <svg
                    class="help-icon-large"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="2"
                  >
                    <circle cx="12" cy="12" r="10" />
                    <path d="m9,9a3,3 0 1,1 6,0c0,2 -3,3 -3,3" />
                    <path d="m12,17 l.01,0" />
                  </svg>
                </div>
              </div>
            </div>
          </div>
        </div>

        <!-- Scroll Indicator -->
        <div class="scroll-indicator" id="scrollIndicator">
          <svg
            width="32"
            height="32"
            viewBox="0 0 24 24"
            fill="none"
            stroke="#140b29"
            stroke-width="2"
          >
            <polyline points="6,9 12,15 18,9" />
          </svg>
        </div>
      </div>

      <!-- FAQ Cards Section -->
      <div class="faq-section">
        <div class="faq-grid" id="faqGrid">
          <div class="faq-card security" data-id="1">
            <div class="card-content">
              <div class="card-header">
                <div class="icon-container">
                  <svg
                    class="icon"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="2"
                  >
                    <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z" />
                  </svg>
                </div>
                <div>
                  <h3 class="card-question">How can I join the APP Team?</h3>
                </div>
              </div>

              <div class="card-answer">
                <div class="answer-content">
                  <p class="answer-text">
                    You can join by filling out the recruitment form we share at
                    the start of each semester. Follow our social media for
                    updates!
                  </p>
                </div>
              </div>
            </div>
          </div>

          <div class="faq-card wallet" data-id="2">
            <div class="card-content">
              <div class="card-header">
                <div class="icon-container">
                  <svg
                    class="icon"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="2"
                  >
                    <path d="M21 12V7H5a2 2 0 0 1 0-4h14v4" />
                    <path d="M3 5v14a2 2 0 0 0 2 2h16v-5" />
                    <path d="M18 12a2 2 0 0 0 0 4h4v-4Z" />
                  </svg>
                </div>
                <div>
                  <h3 class="card-question">
                    Do I need any prior experience to join?
                  </h3>
                </div>
              </div>

              <div class="card-answer">
                <div class="answer-content">
                  <p class="answer-text">
                    No! We welcome beginners. If you're eager to learn, we'll
                    help you grow with training and mentorship.
                  </p>
                </div>
              </div>
            </div>
          </div>

          <div class="faq-card speed" data-id="3">
            <div class="card-content">
              <div class="card-header">
                <div class="icon-container">
                  <svg
                    class="icon"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="2"
                  >
                    <polygon points="13,2 3,14 12,14 11,22 21,10 12,10 13,2" />
                  </svg>
                </div>
                <div>
                  <h3 class="card-question">
                    What kind of projects do you work on?
                  </h3>
                </div>
              </div>

              <div class="card-answer">
                <div class="answer-content">
                  <p class="answer-text">
                    In the App Team, we develop apps for college fests like
                    Nimbus and Hillfair, build websites, and organize the annual
                    Hack on Hills hackathon.
                  </p>
                </div>
              </div>
            </div>
          </div>

          <div class="faq-card support" data-id="4">
            <div class="card-content">
              <div class="card-header">
                <div class="icon-container">
                  <svg
                    class="icon"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="2"
                  >
                    <path d="M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2" />
                    <circle cx="9" cy="7" r="4" />
                    <path d="m22,21-3-3m2-5a5,5 0 1,1-10,0 5,5 0 0,1 10,0Z" />
                  </svg>
                </div>
                <div>
                  <h3 class="card-question">
                    Can first-year students join the team
                  </h3>
                </div>
              </div>

              <div class="card-answer">
                <div class="answer-content">
                  <p class="answer-text">
                    Yes! First-year students are encouraged to join and explore
                    different domains to find what interests them most.
                  </p>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- Bottom spacing -->
      <div class="bottom-spacing"></div>
    </div>

    <%- include("./partials/footer") %>
    <script>
      // Scroll Animation Observer
      const observerOptions = {
        threshold: 0.1,
        rootMargin: "0px 0px -50px 0px",
      };

      const observer = new IntersectionObserver((entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            const delay = entry.target.dataset.delay || 0;
            setTimeout(() => {
              entry.target.classList.add("fade-in-up");
            }, delay);
            observer.unobserve(entry.target);
          }
        });
      }, observerOptions);

      // Observe all domain cards
      document.addEventListener("DOMContentLoaded", () => {
        const animatedElements = document.querySelectorAll("[data-animate]");
        animatedElements.forEach((el) => observer.observe(el));

        // Add scroll reveal class to elements
        const scrollElements = document.querySelectorAll(".domain-card");
        scrollElements.forEach((el) => el.classList.add("scroll-reveal"));
      });

      // Smooth scroll behavior
      document.documentElement.style.scrollBehavior = "smooth";

      // Simple mouse parallax for hero devices
      const heroDevices = document.querySelector(".hero-devices");
      document.addEventListener("mousemove", (e) => {
        if (heroDevices) {
          const x = (e.clientX / window.innerWidth - 0.5) * 100;
          const y = (e.clientY / window.innerHeight - 0.5) * 100;
          heroDevices.style.transform = `translate(${x}px, ${y}px)`;
        }
      });
      document.addEventListener("mouseleave", () => {
        if (heroDevices) {
          heroDevices.style.transform = "";
        }
      });
      const sections = [
        {
          id: "hack-on-hills",
          title: "Conduct Hack on Hills",
          description:
            "North India's biggest student-run hackathon, powered by our robust event management and judging platform with real-time updates and seamless participant experience.",
          image: "/homepage/hoh.webp",
        },
        {
          id: "nimbus-app",
          title: "Nimbus App",
          description:
            "A comprehensive platform for NIT Hamirpur's annual tech fest, offering schedules, live updates, interactive maps, and streamlined event registration features.",
          image: "/homepage/nimbus.webp",
        },
        {
          id: "hillfair-app",
          title: "Hillfair App",
          description:
            "The official app for NIT Hamirpur's cultural fest, blending vibrant UI with seamless user experience to celebrate student creativity and cultural diversity through innovative features.",
          image: "/homepage/hillfair.webp",
        },
      ];

      let activeSection = 0;
      const currentImage = document.getElementById("currentImage");
      const sectionElements = document.querySelectorAll(".section");

      function handleScroll() {
        const scrollPosition = window.scrollY;
        const windowHeight = window.innerHeight;

        const sectionElements = document.querySelectorAll(".section");
        let newActiveSection = 0;

        sectionElements.forEach((section, index) => {
          const rect = section.getBoundingClientRect();

          // Change image as soon as section starts entering viewport
          if (rect.top <= windowHeight * 0.8) {
            newActiveSection = index;
          }
        });

        if (newActiveSection !== activeSection) {
          activeSection = newActiveSection;
          updateImageAndSections();
        }
      }
      const scrollPosition = window.scrollY;
      const windowHeight = window.innerHeight;
      const middleOfScreen = scrollPosition + windowHeight / 2;

      // Header takes up first viewport
      const headerHeight = windowHeight;

      if (middleOfScreen < headerHeight) {
        activeSection = -1; // No section active in header
      } else {
        // Calculate which section's middle point is closest to screen middle
        const scrollAfterHeader = middleOfScreen - headerHeight;
        const sectionHeight = windowHeight * 0.8;

        // Find which section the middle of the screen is currently viewing
        const currentSectionIndex = Math.floor(
          scrollAfterHeader / sectionHeight
        );

        // Ensure we don't go beyond available sections
        const validSectionIndex = Math.max(
          0,
          Math.min(currentSectionIndex, sections.length - 1)
        );
        activeSection = validSectionIndex;
      }

      updateImageAndSections();

      function updateImageAndSections() {
        // Update image based on active section
        if (activeSection >= 0 && activeSection < sections.length) {
          const newImageSrc = sections[activeSection].image;
          if (currentImage.src !== newImageSrc) {
            currentImage.classList.add("changing");
            setTimeout(() => {
              currentImage.src = newImageSrc;
              currentImage.classList.remove("changing");
            }, 200);
          }
        }

        // Update section states
        sectionElements.forEach((section, index) => {
          if (index === activeSection) {
            section.classList.remove("inactive");
            section.classList.add("active");
          } else {
            section.classList.remove("active");
            section.classList.add("inactive");
          }
        });
      }
      // Update image
      if (activeSection >= 0 && activeSection < sections.length) {
        currentImage.src = sections[activeSection].image;
      } else {
        currentImage.src = sections[0].image;
      }

      // Update section states
      sectionElements.forEach((section, index) => {
        if (index <= activeSection) {
          section.classList.remove("inactive");
          section.classList.add("active");
        } else {
          section.classList.remove("active");
          section.classList.add("inactive");
        }
      });

      // Add scroll event listener
      window.addEventListener("scroll", handleScroll);

      // Call once to set initial state
      handleScroll();
      let scrollY = 0;
      let hoveredCard = null;

      // Handle scroll effects
      function faqhandleScroll() {
        const faqSection = document.querySelector(".faq-hero-section");
        const heroContent = document.getElementById("faqheroContent");
        const scrollIndicator = document.getElementById("scrollIndicator");
        const faqGrid = document.getElementById("faqGrid");

        // Get the distance from the top of the FAQ section to the top of the viewport
        const faqRect = faqSection.getBoundingClientRect();
        const relativeScroll = Math.max(0, -faqRect.top);

        // Hero section effects (use relativeScroll)
        const heroOpacity = Math.max(0, 1 - relativeScroll / 900);
        const heroScale = Math.max(0.85, 1 - relativeScroll / 1800);

        heroContent.style.opacity = heroOpacity;
        heroContent.style.transform = `scale(${heroScale})`;
        scrollIndicator.style.opacity = heroOpacity;

        // FAQ cards effects (use relativeScroll)
        const cardsTranslateY = Math.max(0, 100 - relativeScroll / 12);
        const cardsOpacity = Math.min(1, relativeScroll / 900);

        faqGrid.style.transform = `translateY(${cardsTranslateY}px)`;
        faqGrid.style.opacity = cardsOpacity;
      }

      // Initialize
      window.addEventListener("scroll", faqhandleScroll);
      document.addEventListener("DOMContentLoaded", () => {
        setupCardHovers();
        faqhandleScroll(); // Initial call
      });

      // Smooth scrolling for any internal links (if needed)
      document.addEventListener("click", (e) => {
        if (e.target.closest('a[href^="#"]')) {
          e.preventDefault();
          const target = document.querySelector(
            e.target.closest("a").getAttribute("href")
          );
          if (target) {
            target.scrollIntoView({ behavior: "smooth" });
          }
        }
      });
    </script>
  </body>
</html>
